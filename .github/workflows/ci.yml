name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    name: Syntax check & smoke test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['18', '20', '22']

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Check syntax — bin/kalos.mjs
        run: node --check bin/kalos.mjs

      - name: Smoke test — --version
        run: node bin/kalos.mjs --version

      - name: Smoke test — --help
        run: node bin/kalos.mjs --help

      - name: Smoke test — unknown command exits with error
        run: |
          node bin/kalos.mjs unknown-command 2>&1; [ $? -ne 0 ] && echo "Correctly errored" || (echo "Should have exited non-zero" && exit 1)
